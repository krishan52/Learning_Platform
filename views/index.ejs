<%- include('partials/header') %>

    <section id="top-jumbotron" class="jumbotron text-center">
      <div class="container">
        <h1 class="jumbotron-heading">Album example</h1>
        <p class="lead text-muted">Something short and leading about the collection below—its contents, the creator, etc. Make it short and sweet, but not too short so folks don't simply skip over it entirely.</p>
        <p>
          <a href="#" class="btn btn-primary my-2">Main call to action</a>
          <a href="#" class="btn btn-secondary my-2">Secondary action</a>
        </p>
      </div>
    </section>

    <div class="album py-5 bg-light">
      <div class="container">
        <h2>Latest Courses > </h2>
        <div class="row">

          <% courses.forEach(function(course) { %>
          <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
              <img class="card-img-top" src="/images/teacher.jpeg" alt="Card image cap">
              <div class="card-body">
                <a href="/courses/<%= course._id %>/details"><h3><%= course.title %></h3></a>
                <p class="card-text"><%= course.description %></p>
                <div class="d-flex justify-content-between align-items-center">
                  <div class="btn-group">
                    <a class="btn btn-sm btn-outline-secondary" href="/courses/<%= course._id %>/details">View Course</a>
                  </div>
                  <small class="text-muted">9 mins</small>
                </div>
              </div>
            </div>
          </div>
          <% }); %>

        </div> <!-- close row -->
      </div> <!-- close container -->
    </div> <!-- close album py5 -->



<%- include('partials/footer') %>
